#include<bits/stdc++.h>

using namespace std;
int n;
main()
{
	int t;
	cin >> t;
	while(t--)
	{
		vector<long long> v;
		cin >> n;
		long long a[n+5]={0};
		for(int i=0;i<n;i++) cin >> a[i];
		int i=0;
		while(i<n)
		{
			int ok=0;
			for(int j=i+1;j<n;j++)
			{
				if(a[j]>a[i])
				{
					for(int k=j+1;k<n;k++)
					{
						if(a[k]<a[j])
						{
							v.push_back(a[k]);
							ok=1;
							break;
						}
					}
					break;
				}
			}
			if(ok==0) v.push_back(-1);
			i++;
		}
		for(int i=0;i<v.size();i++) cout << v[i] << " ";
		cout << '\n';
	}
}
